# Copy this file to `.env.local` and adjust per environment
VITE_API_BASE=https://api.example.com
# Optional: preset a deterministic login code for local mock WeChat service
# VITE_DEV_LOGIN_CODE=wx-dev-customer
